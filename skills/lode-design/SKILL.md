---
name: lode-design
description: "Lodestar 主线③设计稿与原型(可选)。基于 Design Brief 生成高保真设计稿和可交互原型。当需要在编码前看到真实界面、做可点击原型验证交互时使用。骨架先行、防偷加功能。Trigger: /lode-design"
---

# Design Maker（设计稿 & 原型图 · 可选）

主线第 ③ 环（可选）。把 `design-brief.md` 变成**高保真设计稿 / 可交互原型**，让人在写正式代码前就能看到、点到真实界面。

## Usage（什么时候用）

- `design-brief.md` 已就绪，且界面是产品成败的关键，值得先验证。
- 用户希望"先看到东西"再决定要不要这么做。
- 交互较复杂，静态描述说不清，需要可点击原型对齐预期。

## 怎么出（骨架先行）

- **先骨架，再增量**：先出最薄的可跑骨架，确认后再一层层加。**别一次让它出太复杂的**——容易崩，还会在各页藏一堆你没要的功能，回头删改非常麻烦。
- 可借力已装的 `open-design` MCP 或 `frontend-design` / `imagegen-frontend-web` 等技能承载生成。

## Done（什么算合格）

产出 `.lode/<project>/mockups/`（HTML/JSX 等可在浏览器跑的高保真件），且满足：
- 严格落地 Design Brief 的 token 与设计方向，不另起一套风格。
- 覆盖核心流程的关键页面与关键状态（空/载入/错误）。
- 原型可点击跑通主流程，不是一堆死图。
- 与 product-spec 的范围一致，不多画范围外的页面。
- **列出"额外加了什么"**：把 design-brief 之外自己补的元素单独列出，交用户确认/删，保持可控。
- 产出的**设计代码可直接交给开发复用**（handoff 到终端 / 导出工程包放进项目目录），让 lode-build 在其基础上直接开发，而不是重写。

## Guardrails（红线）

- 这是**可选环**：界面不关键时直接跳过，不要为做而做。
- **不偷加功能**：超出 design-brief 的元素必须显式标出，不默默塞进去。
- 原型是为对齐预期，不是最终产物——不要在这里堆生产级工程结构。
- 不偏离 Design Brief 自由发挥；要改方向先回 `/lode-brief`。
- 产出先给用户确认再进 `lode-plan`。

## → 下一步
原型确认 → `/lode-plan` 拆切片。
